---
- name: Configure linux roles
  hosts: linux
  become: true

  vars:
    ansible_user: "{{ my_ansible_user }}"
    desktop_user_names: "{{ desktop_users | map(attribute='name') | list }}"

  roles:
    - claude_code
    - role: google_chrome
      tags: not-supported-on-vagrant-arm64

    # Optional roles - uncomment as needed
    #- cursor_ide
