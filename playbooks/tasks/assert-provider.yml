---
- name: Assert provider is supported
  ansible.builtin.assert:
    that: provider in ['tart', 'docker', 'hcloud']
    fail_msg: >-
      Unsupported provider '{{ provider }}'. Allowed values: tart, docker, hcloud.
      {{ assert_provider_usage_hint }}
