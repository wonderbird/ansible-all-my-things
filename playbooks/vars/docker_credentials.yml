---
# Default root password baked into the ansible-vm-docker:latest image at
# build time (via chpasswd). This is a default credential for a local,
# disposable container — not a project secret.
# To change: remove the existing ansible-vm-docker:latest image to force a
# rebuild with the new value (the image is otherwise built once and reused).
docker_root_password: docker
