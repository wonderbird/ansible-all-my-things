---
# Default credentials for ghcr.io/cirruslabs/ubuntu:24.04 OCI images.
# This is the publicly documented image default — not a project secret.
# To change: inject a cloud-init user-data seed before tart run (nocloud datasource).
tart_admin_password: admin
