---
# AWS Provision Playbook
# Provisions and configures AWS EC2 development environment

- name: Provision AWS EC2 Instance
  import_playbook: provisioners/aws-ec2.yml

- name: Configure AWS Development Environment
  import_playbook: configure.yml
  vars:
    target_inventory: inventories/aws/aws_ec2.yml
