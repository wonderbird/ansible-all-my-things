---
# AWS-specific overrides for all AWS instances
# These override platform defaults for AWS provider

# AWS Linux instances use 'ubuntu' as admin user
admin_user_on_fresh_system: "ubuntu"

# AWS-specific configuration
aws_region: "eu-north-1"
aws_availability_zone: "eu-north-1a"