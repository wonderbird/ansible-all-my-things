# The dynamic inventory for Hetzner Cloud servers running Linux
# does not assign the hcloud_linux group automatically.
# Therefore, we create a static inventory file that assigns
# the servers to the hcloud_linux group.
---
all:
  children:
    hcloud_linux:
      hosts:
        hobbiton:
