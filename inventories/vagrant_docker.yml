# This file is a copy of the inventory generated by Vagrant
# Steps to regenerate this file:
#
# 1. comment the custom inventory configuration in /test/docker/Vagrantfile
# 2. run `vagrant up`
# 3. copy the generated inventory from .vagrant/provisioners/ansible/inventory/vagrant_ansible_inventory
# 4. paste it here
# 5. adapt path to the SSH key file so that it is relative to this file
# 6. uncomment the custom inventory configuration in /test/docker/Vagrantfile
# 7. run `vagrant provision` to ensure everything works as expected

dagorlad ansible_host=0.0.0.0 ansible_port=2223 ansible_ssh_private_key_file='../test/ssh_user_key/id_ecdsa'

[linux]
dagorlad

[vagrant_docker]
dagorlad

[linux:vars]
admin_user_on_fresh_system=vagrant
