# This file is a copy of the inventory generated by Vagrant
# Steps to regenerate this file:
#
# 1. Comment the custom inventory configuration in /test/tart/Vagrantfile
# 2. Run `vagrant up`
# 3. Check the inventory in .vagrant/provisioners/ansible/inventory/vagrant_ansible_inventory
# 4. Update the inventory here, but use YAML format as described here: https://docs.ansible.com/ansible/latest/inventory_guide/intro_inventory.html
# 5. Adapt path to the SSH key file so that it is relative to this file
# 6. Uncomment the custom inventory configuration in /test/tart/Vagrantfile

# TODO: is the SSH private key file path correct? How to update the IP address automatically after a `vagrant up`?
# Consider a python vagrant inventory script, e.g.
# - Python 3 version of Lorin's version by Joris Roovers: https://github.com/jorisroovers/casa/blob/main/legacy/inventory/vagrant/vagrant.py
# - Lorin Hochstein's version is quite nice: https://gist.github.com/lorin/4cae51e123b596d5c60d
#lorien ansible_host=192.168.64.51 ansible_port=22 ansible_user='admin' ansible_ssh_private_key_file='.vagrant/machines/lorien/tart/private_key'
#
#[linux]
#lorien
#
#[vagrant_tart]
#lorien

all:
  hosts:
    lorien:
      ansible_host: 192.168.64.51
      ansible_port: 22
      ansible_user: 'admin'
      ansible_ssh_private_key_file: 'test/ssh_user_key/id_ecdsa'

linux:
  hosts:
    lorien:

vagrant_tart:
  hosts:
    lorien:
