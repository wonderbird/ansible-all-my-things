#SPDX-License-Identifier: MIT-0
---
# The base URL for the Claude Code manifest file has been taken from
# https://code.claude.com/docs/en/setup#binary-integrity-and-code-signing
claude_code_manifest_base_url: "https://storage.googleapis.com/claude-code-dist-86c565f3-f756-42ad-8dfa-d59b1c096819/claude-code-releases"
claude_code_platform_map:
  x86_64: linux-x64
  aarch64: linux-arm64

# Pinned Claude Code release version (STRIPPED, no "v" prefix -- matches the
# manifest URL's own path convention, e.g. ".../claude-code-releases/2.1.204/").
# See https://github.com/anthropics/claude-code/releases for available versions.
# Updated automatically by playbooks/update-versions/perform-updates.yml.
claude_code_version: "2.1.278"

# SHA-256 checksums for the linux binaries (x64 and arm64), taken from the
# pinned version's own manifest.json (claude_code_manifest_base_url above).
# All three values (version + both checksums) must be updated together.
claude_code_sha256_linux_x64: "5c4735937844e84f8a93306e841a5b0e12252909b07870f789b190468da147ab"
claude_code_sha256_linux_arm64: "7de6cab134e48321148e30182c98614118e8f4666819412bead45865190b34ed"
