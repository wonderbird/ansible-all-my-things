#SPDX-License-Identifier: MIT-0
galaxy_info:
  author: Stefan Boos
  namespace: wonderbird
  role_name: tmux
  description: Install and configure tmux with plugins on Linux
  company: n.a.
  issue_tracker_url: http://github.com/wonderbird/ansible-all-my-things/issues
  license: MIT
  min_ansible_version: 2.19
  galaxy_tags: []

# install-plugins.yml's TPM/catppuccin/tmux-cpu clone tasks unconditionally
# invoke ansible.builtin.git -- a hard runtime dependency on the git binary
# that only the git role provisions on target hosts. See
# docs/architecture/concepts/role-dependency-declaration.md.
dependencies:
  - git
